{
  "name": "cap-storage",
  "version": "3.0.2",
  "author": "Diego SanMo",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "otherbuild": "rm -rf dist && tsc",
    "transpile": "ngc",
    "package": "rollup -c",
    "minify": "uglifyjs dist/bundles/capstorageaws.umd.js --screw-ie8 --compress --mangle --comments --output dist/bundles/capstorageaws.umd.min.js",
    "build": "npm run transpile && npm run package && npm run minify && cp README.md dist/README.md && cp -r ./assets dist/assets"
  },
  "dependencies": {
    "@angular/core": "^8.2.13",
    "rxjs": "^6.5.3",
    "zone.js": "^0.9.1"
  },
  "devDependencies": {
    "@angular/compiler": "^8.2.13",
    "@angular/compiler-cli": "^8.2.13",
    "aws-sdk": "^2.564.0",
    "rollup": "^0.62.0",
    "typescript": "^3.5.1",
    "uglify-js": "^3.4.2"
  }
}
